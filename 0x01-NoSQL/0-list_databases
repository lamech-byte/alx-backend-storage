// 0-list_databases
// Connect to the MongoDB server
var conn = new Mongo();
// Get the admin database
var db = conn.getDB("admin");
// List all databases with size
db.runCommand({ listDatabases: 1, showSize: true }).databases.forEach(db => print(db.name + "\t" + db.sizeOnDisk));
